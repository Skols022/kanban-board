import { FC } from 'react';

import Search from '@/components/shared/Search/Search';

const Header: FC = () => {
  return (
    <header>
      <Search />
    </header>
  );
};

export default Header;
